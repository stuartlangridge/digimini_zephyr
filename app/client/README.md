# Web Client

## Setup

```bash
cd app/client
npm install
pip install watchdog
```

## Development

```bash
python watch.py
```

This builds the web client into `_build/`, serves it on `localhost:5173`,
and live-reloads on file changes.
